class Solution {
    public void reverseString(char[] s) {
       Stack<Character> stk = new Stack<>();
       for(char c:s){
           stk.push(c);
       }
       
       int i=0;
            while(!stk.isEmpty()){
                s[i] = stk.pop();
                i++;
            }
    }
}
